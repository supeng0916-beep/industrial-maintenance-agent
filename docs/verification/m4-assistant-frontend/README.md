# D8 助手前端验收（2026-09-20）

环境：前端 vite dev（5179，/api 代理8765）；后端 serve_api.py --port 8765；模型 deepseek-chat（.env云配置）。业务库无采集器运行，监控区如实显示"未能核实"（既有降级行为，与助手无关）。

## 浏览器实测（IAB，DOM断言+截图）

1. **正常问答**：提交"按资料，20hp变频器输出为额定12.5%时的典型效率是多少？"→ 状态徽章"已回答"，答案 **86%**（整表回读，标注TS11 Table 1与典型值限定），证据区含3条候选（doe-motor-ts11/ts12，每条带"来源文件"http链接与"适用性未核实"标签），显示查询时刻。截图：`2026-09-20-assistant-panel-answered.png`。
2. **证据不足问答**：提交"WEG W22系列15千瓦电机的轴承润滑周期是多少？"→ 内容诚实拒答（"本地知识库中没有找到…无法给出"，并列出检索情况与候选来源全部为DOE历史指南）。**观察项**：状态徽章显示"已回答"而非"证据不足"——模型未自标 insufficient_evidence 标志，内容正确但分类依赖模型自评；记录进D9场景集（场景应断言内容诚实，状态分类作为观察项）。
3. **失败问答**：停止后端后提交 → 错误卡片"本次提问未能完成 服务返回 502"，无结果区残留，提问按钮恢复可用（vite代理返回502被客户端正确分类）。

## 组件/客户端测试（vitest）

`assistantModel.test.ts`（4）、`assistantClient.test.ts`（3）、`AssistantPanel.test.tsx`（7）：合同解析、严格请求负载（不含工具结果/模型配置）、422/503/网络错误分类、六状态渲染、javascript:链接拒绝、空证据不显示0。全套 `npm test -- --run` 47项通过；`npm run check` 无类型错误；`npm run build` 成功。

## 交互备注

浏览器自动化期间发现：看板每2秒轮询引起持续重渲染，Playwright常规actionability点击会超时；验收用页面内原生事件派发完成（React受控组件native setter + input事件 + 原生button.click()）。人工使用不受影响。
