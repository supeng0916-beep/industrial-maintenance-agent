# 看板启动与可用性修复验收

## 原因与处理

- 本项目的前端/API未在运行，127.0.0.1:5173连接被拒绝。
- IPv6 [::1]:5173 属于另一个项目的 Vite；未停止或修改它。
- 新增 run_dashboard.py：统一启动模拟器、采集器、API、前端；默认15030/8010/5175，自动配置前端代理，使用独立演示库；检查端口占用与就绪，Ctrl+C清理同次子进程及其进程组。
- 根据用户指定的 frontend-design 技能（anthropics/skills 官方仓库，已读取）重新组织仪表区域、中文提示、使用说明、技术细节与窄屏布局。

## 实测

- 新增启动器测试先失败（文件未实现），实现后通过：端口占用不停止原服务；真实全栈可通过网页代理读取65.3℃；退出释放三个端口。
- Python全项目19项测试通过，14.164秒，含原Modbus/API/OPC UA与新增启动器测试。
- 前端8项测试通过；TypeScript检查和生产构建通过。图表库造成735.74KB构建包提醒，尚未做分包，不影响本机启动。
- 真实浏览器访问 http://127.0.0.1:5175 ，显示65.3℃、原采集时间持续更新、真实历史点数增加。
- 1440px和390px检查：页面宽度分别1425px和375px，均不超出视口；使用说明可展开，三步阅读指引与启动命令可见。
- 独立临时前端5176代理到不可用后端，验证“接口请求失败，当前状态未能核实”、可执行排查建议，且不把失败当零温度。不曾取得历史时明确“暂时无法获取历史记录”，不误称数据库没有数据。临时服务和页面已清理。
- 保留5175的本地演示供用户打开，使用data/dashboard-demo.sqlite3；未修改data/my-learning.sqlite3。主启动会话仍运行，课程结束应停止以免持续采集。
