# 原始真实语料核查记录

日期：2026-09-18。15份独立原PDF实际下载，30页，SHA256见source-register；原件未修改。英文正文与原页layout文本均保留，未使用AI生成正文。

## 实际发现与修复

1. 直接extract_text会把右栏建议插入左栏句子：改为固定几何分栏，每页主栏后接侧栏。页2从y=50读取，保留开头文字与脚注标记；每页原layout文本另存以追溯。
2. 表格在正文中保留layout代码块，表题、列头、脚注及条件不靠AI补写。检查了TS04 p2（RPM、平行偏移mils、角度mils/inch、两组Excellent/Acceptable）、TS07 p1（负载与三列不平衡）、TS11 p2（负载百分比/效率百分比，5/100hp等行，typical/无统一比较协议脚注）。
3. TS03 p2 的L10/L10h下标乱序：对照渲染精确恢复为L10、L10h。TS03 p1 wind- ing、TS04 p1 eleva- tions、TS11 p2 adjust- able断行拼写按原词恢复。没有全局删除连接号，避免损坏真实复合词。
4. TS07 p1电压不平衡示例分数恢复为 `((460 – 455) / 460) x 100 = 1.1%`；p2温升公式平方显式转写为 `(% Voltage Unbalance)^2/100`。
5. TS11 p1功率/速度/流量公式下标恢复，用 `_1/_2`、`^3`表达；变量定义与原图一致。p2系统效率乘积恢复为η_system=η_VFD×η_Motor×η_Equipment。精确规则在normalize.py。
6. TS07页脚存在隐藏draft叠字导致错误提取August及残缺DOE编号。渲染可见版次为November 2012、DOE/GO-102012-3733，据此修正元数据。
7. TS15 p2图内旋转轴标题和重复文字被extract_text放到正文结尾，已排除图形区域，保留caption、Fluke来源及后续解释。没有把曲线转成新数值表。

## 视觉抽检范围

实际渲染并查看：TS03 p1、p2；TS04 p2；TS07 p1、p2；TS11 p1、p2；TS14 p2；TS15 p1、p2。PNG在pages，qa-contact-sheet.png展示其中6页。原PDF图表文字可见，未修改源版面。

这不是全30页每个字符的完美校勘声明。其余公式、图表可能仍有PDF文本层固有限制；精确计算/图形读数须回看原PDF。本批指南年份较旧，仅作带版本的知识检索资料，不当作现行强制标准或设备现场状态。

## 统计与复现

仅manifest列出的15个MD可入库；相同PDF hash无重复。registry记录30页的坐标区域、表格数量和原件哈希。下载脚本不覆盖已有原PDF；提取脚本可重复生成同输入的MD及manifest，未触碰原教学知识库/数据/服务/评测。
