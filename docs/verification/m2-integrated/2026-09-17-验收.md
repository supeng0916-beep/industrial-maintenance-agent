# M2 四测点综合场景验收（2026-09-17）

本次在用户授权的四测点练习服务运行真实协议场景，使用 data/four-points-practice.sqlite3，保留已有历史。未重新运行所有单元测试；此前开发报告与本次现场验收分开看待。

## 实测通过

1. 状态7改回1，65.3℃正常采集恢复。
2. 温度改为81℃，进入pending。
3. pending期间状态改7，产生data_validation_error，pending清空，未产生告警。
4. 状态改回1，从新的首个高温样本计时，连续观测满5秒后触发告警#1。
5. 暂停本次模拟器进程（SIGSTOP），造成协议响应超时，产生communication_error，超过5秒数据过期；告警#1保留、当前未知。该实验为无响应超时，不等同于TCP连接拒绝。
6. SIGCONT恢复模拟器，采集恢复且仍是告警#1。
7. 停止并重启本套服务，使用同一数据库及81℃；仍是#1，总数不增加。
8. 改77.9℃，告警恢复，历史共1条保留。浏览器核对四测点有效、77.9℃、1.23A、1450rpm、运行，当前无未解除告警。

原始响应证据见同目录 2026-09-17-live.jsonl。判断通过使用实际API响应断言，末态另核对浏览器。

## 当前练习入口

http://127.0.0.1:5180 ，API8016，模拟器15035。旧5179在快速重启时暂时无法绑定，改用上述端口；未清理或占用其他练习服务。启动raw810后通过协议写入779，当前模拟温度77.9℃。服务持续运行与否需使用时检查。

## 结论与边界

四测点采集与温度告警主要综合场景通过。不据此宣称所有M2学习验收完成：项目计划中的跨寄存器编码练习尚未进行；修改规则/排错的独立掌握仍需学习者练习。数据库写失败回滚由此前开发自动测试覆盖，本次未对练习库注入损坏或写入故障。实际工业硬件与同步采样未验证。
