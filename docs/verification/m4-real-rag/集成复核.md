# M4.2 主任务集成复核（2026-09-18）

独立复核最终 `doe-e5-final`、`doe-bge-final` 及44题结果；代码策略为 `rag-page-heading-sentence-v4`。本记录不替代完整回归日志，也不评价尚未实现的LLM回答。

## 已核查

- 两索引语料manifest与冻结哈希一致；44题文件与评测记录哈希一致。
- 每篇按字符范围还原，15篇正文均完整覆盖，原文片段与源文件逐字一致。
- E5为113块、最大512 tokens；BGE为131块、最大508 tokens。块页码为对应单个物理页。
- TS11第二页不再继承第一页局部“no static lift”条件；不因这一检查宣称所有复杂表格语义完整。
- 用评测器逐题重算Hit@5/10，与保存结果一致；10道证据不足题指标为null，没有混入有答案题的分母。
- 独立新进程执行真实E5 CLI查询“按典型PWM变频器效率表，20hp驱动器在12.5%负载时效率是多少？”，排名第一返回TS11第2页和原始PDF URL。见 `integration-cli-query.json`。

统计和逐项结论见 `integration-review.json`。分块原文完整覆盖不等同所有条件都在单块内，固定证据短语命中也不是回答正确率。

## 具体样例复核

| 题目 | 实际观察 | 可得结论 |
| --- | --- | --- |
| R31：20hp、12.5%负载典型效率 | E5第一条是TS11第2页表格 | 找到表内86%的依据；仍需保留“典型值”，不能当指定品牌保证 |
| R34：100hp替换示例年节电量 | E5第8条才出现标注表格 | top5漏掉该证据，top10命中；扩大候选有帮助，但不证明其他漏检都能解决 |
| R03：实验室效率能否代表现场 | top10有正确文档部分片段，但没有预标记短语 | 标注证据未命中；不能据此断言候选完全没有相关信息 |
| N03：ABB M3BP 160润滑脂和加脂量 | 仍返回10个DOE候选，首条是TS02第2页 | 当前库缺该型号维护表；返回候选并未填补型号资料缺口 |
| N06：电机85℃能否确定润滑不足 | 仍返回候选，其中有电机寿命类内容 | 一般资料不能把单一温度变成已确定的现场根因 |

N03/N06为本次人工抽查，未把其余8道证据不足题改成“人工审核通过”，未计算自动拒答成功率。原始评测JSON仍保留 `manual_relevance=not_reviewed`，人工观察单独记在此处。

## 当前判断

E5配置在本批中文问题/英文指南上优于BGE中文配置，可作为后续实验起点；保留集Hit@5仅3/10、Hit@10为5/10，当前质量不足以据此宣称可靠问答。两套tokenizer切块不同，不能把差异全部归于权重。

本轮止于可复跑检索基线。下一轮先分析失败原因，选一个改进做对照，并新增保留题；不在已观察的保留题上反复调参后继续称为独立测试。
