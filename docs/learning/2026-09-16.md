# 2026-09-16｜从设备数据到可信告警

> 整理于 2026-09-17。依据本任务可见对话、用户贴出的终端结果与项目验收记录；早期内容经上下文摘要保留，问答为归纳而非逐字录音。阈值均为教学配置，不代表真实设备安全标准。

## 1. 昨天走到了哪里

其中寄存器等基础概念为前一天知识的延续复习。M1 已贯通：Modbus 模拟器 → 采集器 → SQLite → 只读 API → 看板。独立 OPC UA 实验已完成读取、订阅与命名空间变化验证，但尚未正式接入主项目。M2 已完成温度回差、持续超限确认、告警去重和持久化；电流、转速、运行状态等多测点尚未开发。

最重要的一句话：**系统只能根据取得的证据描述设备，不能把没有数据解释为正常，也不能把通信成功解释为设备健康。**

## 2. 整条链路各自负责什么

| 组件 | 职责 | 常见误解 |
| --- | --- | --- |
| 模拟器 | 模拟 PLC 或温控仪的数据接口，通过真实协议提供数值 | 仿真通信不等于真实电机物理过程 |
| 采集器 | 读取、换算、记录时间、保存测量、运行告警规则 | 页面刷新不是采集 |
| SQLite | 保存测量、失败事件、告警及规则状态 | 历史值不是当前实测值 |
| 后端 API | 从数据库查询并解释数据时效 | GET 查询不应启动采集或补写告警 |
| 前端 | 显示温度、时间、数据可信度、趋势和告警 | API 请求失败不能直接断言设备断线 |
| 未来 Agent | 查询证据和文档，辅助解释与排查 | 不让模型代替确定性告警规则 |

两个浏览器查看同一设备，只应增加查询请求，不应增加采集频率。关闭页面后，采集进程仍可独立运行。

## 3. Modbus：寄存器、地址、列表与倍率

**PLC（可编程逻辑控制器）**：工业控制器，依据输入和程序逻辑控制输出。模拟器目前只模拟其数据通信接口。

**寄存器**：Modbus 中一个通常为 16 位的数据单元。设备可以提供很多寄存器，具体代表温度、电流还是其他数据，由设备点位表约定；不是每个地址都有效，也不是所有值都必然变化。

**保持寄存器（Holding Register）**：Modbus 的一个数据区域，协议有读写功能；具体设备是否允许某地址写入，仍受设备实现限制。另有输入寄存器、线圈、离散输入。

```python
response = client.read_holding_registers(10, count=3, device_id=1)
# 假设返回 [653, 120, 1500]
# registers[0] 对应地址 10
# registers[1] 对应地址 11
# registers[2] 对应地址 12
```

**列表索引不等于寄存器地址。** 本次从地址 0 开始，所以 registers[0] 恰好对应地址 0；从地址 10 开始就对应地址 10。count=3 表示读取三个寄存器，并不表示取三个历史时刻。

一个值可能跨多个寄存器，例如 32 位浮点数；还需要知道字节/字顺序。本项目已有版本只用了单个无符号寄存器。

```python
raw = 653
scale = 0.1
temperature = raw * scale  # 65.3℃
```

Modbus 数字没有自动附带单位和倍率。两者来自点位约定。真实历史是采集器将多次读取结果按时间保存后形成的。

## 4. 成功、失败、历史和时间

| 情况 | 应保存什么 | 不能做什么 |
| --- | --- | --- |
| 读取成功 | 本次真实数值及采集时间 | 仅因数值没变就认定重复数据 |
| 通信失败 | communication_error 事件 | 填 0、复制旧值配新时间 |
| 读取成功但写库失败 | 明确报告存储失败 | 把它说成通信失败或已保存 |
| 长时间没有新记录 | 提示数据过期，检查采集器 | 直接断定传感器坏了 |

**collected_at**：本项目采集器记录的采集时间。**source_time / SourceTimestamp**：数据源提供的时间，可为空，不能捏造。**ServerTimestamp**：OPC UA 服务器关联的时间；**ReceivedAt**：客户端收到通知的时间。二者差值可能包含时钟偏差、排队和传输等因素，不能简单当作纯网络延迟。

**UTC**：统一存储时间使用的时间基准。`+00:00` 表示 UTC，上海通常显示为 UTC+8。例如 `05:16:38+00:00` 对应本地 13:16:38。

**最后成功时间**仅在成功读取后更新；**最后尝试时间**包括失败。**数据年龄**是现在减去最后成功时间，本项目默认大于 5 秒为过期。旧失败事件保留，不代表设备此刻仍离线。

```sql
-- 最新记录在前；同一时间用 id 辅助确定顺序
ORDER BY collected_at DESC, id DESC
```

用户实际完成：5 次成功写入 65.3℃；停止模拟器后产生 3 条失败事件，原温度历史不增加；重启模拟器后，同一采集器再次读到 72.8℃。

## 5. Modbus 与 OPC UA 对照

| 维度 | Modbus | OPC UA |
| --- | --- | --- |
| 定位数据 | 区域、地址、设备标识 | NodeId，命名空间及节点标识 |
| 返回内容 | 位或寄存器，含义依赖点位表 | 可携带类型、质量、时间及属性 |
| 获取方式 | 本项目采用轮询 | 可主动读取，也可订阅变化 |
| 单位与换算 | 通常依赖外部约定 | 可能有 EngineeringUnits 等元数据，仍需核对 |
| 历史 | 采集后自己保存 | 订阅队列不等于历史库；历史访问需服务器支持 |
| 安全 | 本实验普通本地 TCP | 协议具备安全机制，本实验匿名 NoSecurity |

**Node（节点）**：地址空间中的对象、变量或属性等信息元素。**NodeId**：用于标识节点，例如 `ns=2;s=MotorA.Temperature`。其中 ns=2 是命名空间索引，s= 后面是字符串标识。

**Namespace URI**：命名空间的稳定名称；客户端先用 URI 查当前索引，避免写死 ns=2。实验加入额外命名空间后，温度变为 ns=3，客户端仍能读取。稳定 URI 并不替代对节点标识和服务器配置的管理。

**DataType=Double** 表示双精度浮点数。不能仅凭它或单位属性就断言不需要缩放，需看变量定义。

**EngineeringUnits** 是可选的工程单位元数据。本实验并未提供该属性，因此输出明确写 `UnitConvention=℃（文档约定，无EngineeringUnits属性）`。

## 6. 数据质量不是设备健康

**StatusCode** 描述数据的质量/状态：Good、Uncertain、Bad 等；具体问题需要查看状态码。

- Good：数据质量状态良好，不表示电机健康，也不证明传感器绝无问题。
- Bad 或 Uncertain：不能只看数值小于 80℃ 就宣布温度正常。
- 更合适的提示：显示最后数值及时间，并说明质量不可信，当前是否正常无法确认。

本项目 Modbus 的 quality=good 来自采集程序根据成功请求作出的标准化标记，不是假装设备返回了 OPC UA 质量码。

## 7. 订阅：采样、发布、队列和保活

**采样间隔**：服务器检查监视变量的节奏。**发布间隔**：订阅组织并向客户端发送通知的节奏。**监视项（MonitoredItem）**：订阅内具体监视的节点及其参数。**队列**：暂存采样捕获且符合通知条件的数据，大小和丢弃策略影响保留哪些通知。

```text
数据源更新 → 服务器采样/过滤 → 监视项队列 → 订阅发布 → 客户端回调
```

缩短发布间隔，无法找回采样阶段已经漏掉的短时高温。增加队列也不能保存从未采到的值。传感器每秒才更新一次时，更频繁读取不等于获得更多独立测量。

**Keepalive（保活）**：说明订阅仍在响应，不是新温度测量，也不是传感器健康证明。

**回调函数**运行在我们的客户端/采集器程序里，程序退出后不会继续处理通知。重连后要考虑会话、订阅和监视项恢复，并避免重复创建导致重复处理。

“没有通知、主动读取成功”只能证明此次读取路径可用；可能没有满足变化过滤条件，也可能订阅异常，不能仅凭读取成功保证订阅正常。实验相同温度未通知，随后改为 73.5℃ 收到通知，才补充了订阅链路的证据。

实验输出 SourceTimestamp=null 时应保留未知，不能用 ServerTimestamp 或 ReceivedAt 冒充。请求会话超时 3600000ms、服务器给 600000ms，是协商后的超时值，不是看到提示就认定连接失败。

## 8. 告警为什么会抖动

如果 >80℃ 报警、低于80℃恢复，79.8、80.2、79.9、80.1 会使状态反复切换。

你提出使用平均温度，这是合理的平滑方法，但 `70、70、90、70、70` 平均只有 74℃，可能掩盖短时高温。采用哪种方法取决于设备风险和规则要求。

| 方法 | 主要作用 | 注意点 |
| --- | --- | --- |
| 滑动平均 | 平滑数值波动 | 可能稀释尖峰、推迟触发 |
| 回差 | 报警与恢复用不同阈值 | 不解决首次短时超限立刻触发 |
| 持续超限 | 要求满足一段时间才触发 | 需要明确采样中断如何处理 |

**回差（Hysteresis）**：本项目超过 80℃ 触发，低于 78℃ 恢复。已报警后降到 79℃仍报警，恰好78℃也不恢复。未报警时恰好80℃不触发。

## 9. 告警是一段有开始和结束的事件

```text
81℃ → 告警 #1 触发
79℃ → 告警 #1 保持
77℃ → 告警 #1 恢复并保留历史
81℃ → 新一轮符合触发条件后创建告警 #2
```

连续收到十次高温属于同一次告警，不应新建十条记录。告警状态持久化在数据库，重启先识别未解除告警；只在内存里保存会导致重启丢状态。

**曾经回答的易错点**：你正确判断第二次超温后总数为2，但认为两条都报警中。正确是 #1 已恢复、#2 报警中。历史总数与当前未解除数量不是同一指标。

已报警后通信失败：保留未解除告警，同时提示当前温度未知；不能说已经恢复，也不能把旧85℃称为当前温度。

## 10. 事务：把有关联的修改一起保存

**事务（Transaction）**把一组数据库操作作为整体。**提交（COMMIT）**保存整个事务；**回滚（ROLLBACK）**撤销本次未提交的修改。

```python
# 教学伪代码：异常应向外抛出，使上下文回滚
with conn:
    measurement_id = 保存本次温度(85)
    更新告警及评估状态(measurement_id, 85)
```

如果保存告警失败，本次温度插入也应撤销，之前已提交的历史不受影响。设备读取可能成功，但本轮存储失败，需要单独报告。失败事件和取消待确认状态也需要在同一事务里完成。

事务只包含短数据库操作，不要把网络请求或 sleep 放进去长时间持锁。

## 11. 持续超限：5秒不等于5个样本

最终教学规则：有效温度持续超过80℃，连续观测累计至少5秒，才触发。期间≤80℃、通信失败、样本间隔过长或采集器重启，都取消待确认计时。

```text
00秒 第1个高温样本 → 累计0秒
01秒 第2个高温样本 → 累计1秒
02秒 第3个高温样本 → 累计2秒
03秒 第4个高温样本 → 累计3秒
04秒 第5个高温样本 → 累计4秒
05秒 第6个高温样本 → 满足5秒，可触发
```

默认每秒采集，允许相邻样本间隔不超过1.5秒。这个1.5秒是本项目容忍配置，不是协议标准。即使没有失败事件，10秒空白也会打断连续性。连续观测不能证明两个采样时刻之间的物理温度始终超限。

**单调时钟（monotonic）**用于计算持续时长，避免系统日历时间校准导致计时跳动；UTC用于证据日期。待确认计时不跨进程重启续用。

```python
# 概念伪代码：实际实现还包含事务、采集会话和证据保存
if 读取失败:
    取消待确认()  # 已触发告警保持未解除
elif 已有未解除告警:
    if 新有效温度 < 78:
        恢复原告警()
else:
    if 温度 <= 80:
        取消待确认()
    else:
        if 首次超温 or 样本间隔过长:
            从本次样本重新计时()
        if 单调累计时长 >= 5:
            新建告警()
```

**读取失败时先处理失败分支**，不使用不存在的新温度；已触发告警保持未解除。

页面不能靠倒计时自行报警，必须等待后端处理新的有效样本。待确认已累计秒数仅展示已观测证据；没有新样本时保守显示未知。

## 12. 昨天的疑问与回答速查

| 你的疑问或判断 | 可复用的结论 |
| --- | --- |
| registers[0] 就是地址0吗？ | 取决于本次起始地址；索引0是响应第一项 |
| 采集失败，真实温度还会变吗？ | 会；采集失败只是我们不知道 |
| 查到失败事件就是现在离线？ | 看事件与成功记录时间，结合采集状态 |
| 页面不更新就是设备断线？ | 也可能采集器停止、后端/数据库或前端查询出错 |
| Good 是否代表电机正常？ | 不代表；数据质量和设备健康是两个维度 |
| 不到80℃但质量不可信？ | 不能确认正常，保留数值并明确质量问题 |
| 不通知但读取成功？ | 不能断言断线，也不能保证订阅正常；需更多证据 |
| 保活能证明传感器正常？ | 不能，只说明订阅响应 |
| 接收66℃能排除之前超温？ | 不能，可能未采到或未保留 |
| OPC UA 有单位，所以不需要倍率？ | 必须核对变量定义；本实验甚至没有单位属性 |
| 为什么不使用平均温度？ | 可平滑，但可能掩盖尖峰；与回差/持续确认作用不同 |
| 79℃还在报警？ | 已报警且尚未低于78℃，保留状态 |
| 重启后报警状态存哪里？ | 数据库；待确认计时重启则清零 |
| 采集失败能继续累计5秒吗？ | 不能，证据中断；已经触发的告警不因此解除 |
| 两次超温后有两条报警中的记录？ | 先恢复再超温：一条已恢复，一条报警中 |
| SQL里一起成功/失败叫什么？ | 事务，提交或回滚 |

## 13. 实验记录：区分自己操作与开发验证

| 实验 | 结果与证据来源 |
| --- | --- |
| Modbus成功5次、失败3次、恢复72.8℃ | 你贴出的终端输出，成功历史和失败事件分开 |
| OPC UA read / subscribe / 改温度 | 你贴出65.3、72.8、73.5及质量、时间字段 |
| 命名空间变化 | 你贴出ns=3，URI解析后仍读到65.3 |
| 回差与重启 | 本任务实际操作同库：81→79保持；77恢复；81新建#2 |
| 持续超限与采集中断 | 本任务操作独立实验：81待确认，暂停6秒无告警；恢复后从0计时，约5.014秒触发 |
| 整体开发回归 | 开发任务报告35项Python、14项前端测试通过，并完成浏览器场景验证；不是你逐项独立完成 |

学习库：`data/my-learning.sqlite3`。回差实验库：`data/alarm-practice.sqlite3`。持续实验库：`data/sustained-alarm-practice.sqlite3`。这些库应保留，不用它们跑破坏性测试。

昨天页面无法访问时，5176/8011/15031无监听，启动后恢复。另一次快速重启时8011无法绑定但无监听进程，改8012后恢复；没有足够证据把该次原因定为其他应用占用。localhost链接只有对应服务运行时才有效，过去启动成功不保证今天仍运行。

## 14. 关键代码与复习路线

| 文件 | 看什么 |
| --- | --- |
| modbus_reader.py | 读取、异常、倍率转换 |
| collect_temperature.py | 独立循环，成功与失败分支 |
| storage.py | 测量和告警同事务；失败与清pending同事务 |
| alarms.py | 初始化迁移、持续判断、回差、只读状态 |
| api.py | 查询契约与数据时效 |
| frontend/src/AlarmPanel.tsx | 状态与历史的用户表达 |
| experiments/opcua/client.py | 主动读取、订阅及实验字段 |

先复习第9～11节，再阅读 alarms.py 的 evaluate 函数；不要要求自己一次读懂所有SQL和迁移细节。

验收记录：`docs/verification/alarms/验收说明.md`、`docs/verification/sustained-alarms/验收说明.md`。这里记录的是昨天的结果，本笔记未重新运行全套测试。

## 15. 今天的学习与开发起点

继续M2，多测点从“温度+电流”开始。先确认点位表、寄存器地址、倍率、单位和数据类型，再开发读取与展示；第一步只对温度保留已有告警，不擅自增加电流告警阈值。后续再增加转速、运行状态和跨寄存器编码练习。

今日第一道题：从地址0连续读两个寄存器，返回 `[653, 123]`。点位表规定地址0为温度、倍率0.1℃；地址1为电流、倍率0.01A。两个物理量各是多少？如果整个读取请求失败，能否把电流记成0A？

参考答案：温度65.3℃，电流1.23A；读取失败是未知，不是0A。

## 16. 掌握情况与仍需练习

已通过回答或实验表现出理解：倍率、失败不伪造测量、质量不等于健康、订阅保活边界、回差、持久化告警、持续确认中断。

需要再次复习：列表索引与地址；OPC UA单位与缩放不能想当然；事务；历史总数与未解除数量；时间长度与样本数量。代码由AI实现和验证，不据此宣称你已能独立实现全部模块。
