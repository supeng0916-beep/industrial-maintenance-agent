# 独立 OPC UA 教学实验

用户已授权实现；使用已读取的实施计划、测试驱动开发及完成前验证技能，直接执行。

- [x] 新增真实进程验收，先验证实验程序缺失时失败。
- [x] experiments/opcua 内实现 common.py、server.py、client.py；固定 asyncua 2.0.1。
- [x] 服务端仅127.0.0.1，默认4840，URI urn:industrial-maintenance:opcua-lab，MotorA与MotorA.Temperature字符串标识；Double初值65.3，单位文档约定℃。
- [x] 客户端 read / subscribe / set-temperature 三种命令，共用URI解析。set仅本地实验端点，不传设备源时间；订阅1个变量、1个监视项、默认StatusValue，明确请求发布250ms/采样100ms/队列10。
- [x] 验收命名空间移位仍可读、Double转换、初次通知/变值通知/同值观察、Ctrl+C清理。保留原15个测试，保存真实日志并独立审查。
- [x] 独立中文README说明命令、时间来源、无认证加密边界、采样与发布区别及实测局限。

不连接现有Modbus、不读写任何教学数据库、不接入API、不修改docs/learning；测试仅使用随机空闲端口和自己启动的进程。

完成：17项测试通过（13.169秒），独立审查无待修复缺陷。实测使用60573端口，ns3仍按URI读写，订阅初值和72.8通知，同值1.2秒窗口0新通知，源时间null/服务端时间变化如实记录。证据 docs/verification/2026-09-16-opcua-transcript.txt。
