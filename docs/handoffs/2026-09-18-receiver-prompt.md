# 接手Prompt（复制以下正文给你的Agent）

> 编辑注（2026-09-18晚）：用户已改为学习/开发双会话模式，新会话请改用 [2026-09-18-two-session-split.md](2026-09-18-two-session-split.md) 中的两段提示词；本文"不要创建新会话"一句不再适用，其余背景仍有效。D1已于当日完成，见 `docs/verification/m4-rag-improvement-v1/`。

你现在接手我的“工业设备智能运维agent”课程项目，同时负责后续开发与教学。不要创建新的Codex任务或另开会话，我会在当前Agent中与你继续。

## 先读取交付材料

本机项目根目录：`/Users/yuanqi/项目/工业设备智能运维agent`。如果你运行在另一台机器，把它替换成我实际提供的项目目录；不能访问文件时明确告诉我缺什么，不假装已经读过。

请依次读取：

1. `docs/handoffs/README.md`：交接总入口与交付清单。
2. `docs/handoffs/2026-09-18-development-learning.md`：项目背景、我的偏好、已学内容和实际进度。
3. `docs/superpowers/plans/2026-09-18-remaining-development.md`：4阶段、12个工作包，文件、接口设计与验收。
4. `docs/handoffs/2026-09-18-learning-plan.md`：6组主题、10个学习单元。
5. `docs/project-plan-v0.2.md`：总目标与范围，文末日期更新优先于早期设想。
6. `docs/verification/m4-real-rag/验收说明.md`、`检索结果与失败记录.md`、`集成复核.md`、`2026-09-18-e5-query-language-probe.md`。
7. `docs/handoffs/learning-summary-guide.md`：每日总结的跨Agent规范；原版总结Skill及脚本也在交接包内，不依赖Codex安装。项目日总结与个人长期总日志流程分开。

这些文件比原会话是否可访问更重要。不要要求我重新解释整个项目，也不要因为没有Codex特定工具或技能而阻塞。遵循你实际环境的规则，并按小步设计、实现、验证、记录的工程方式执行。

## 你接手时的事实

- M0～M3的Modbus/OPC UA采集、SQLite、监控页面、质量/时效判断、持续超温告警已完成；M4.1三个只读工具已完成。
- M4.2有15份真实DOE英文指南、30页、44题，真实E5/BGE索引和CLI。E5开发集Hit@5=15/24、@10=19/24；已观察保留集3/10、5/10。基线可运行，检索质量不足，不能称为可靠智能问答。
- 最近完整Python回归196项通过；前端旧测试成绩不能冒充本轮重跑。没有生成模型/Agent回答、助手页面和最终课程交付。
- 既有3题中英文探针只是诊断，人工改写存在偏差，不是完整评测或可部署翻译组件。先读结果，不重复把它当新实验。
- 生成模型供应商、本地/API方式和预算尚未决定；到需要时集中问我一次，不假设有密钥或付费授权。

## 我的学习与协作偏好

中文交流。我已有SQL基础和Agent经验，理解比亲手敲代码重要。你可以先开发再教我，我希望看到真实代码中的关键片段、图/伪代码和实际结果。

讲解后可以出一道简短预测、解释或修改题，答对就推进；不要反复问同一种只能答“不能”的题。上一题我已理解：重排只能重新排序召回的候选，候选外的证据无法靠重排找回。不要重考。

我要求使用有来源、可追溯的真实资料，不用少量编造数据证明效果；同时只是课程设计，保持简单，不复用另一方向旧Agent项目，不为追新技术一次堆叠很多组件。

## 工作边界

- 按工作包逐步继续普通可逆开发，不每一步重复询问许可；真实涉及模型选择、预算或缺少必要信息再问。
- 不新建任务/会话，不自动分派其他Agent。不假设原Codex开发任务仍会替你工作。
- 项目目前非Git，不强制初始化。不要改写我的data数据库或停止已有服务；测试用临时/独立演示环境。
- 已有题集、语料、最终索引、成绩保留；新实验写新文件。题目答案不进入知识库，旧holdout已被观察，不能继续称盲测。
- 只读工具，不开放任意SQL/shell/设备写入。数值事实查数据库；文档说明与现场实测分开。没有证据时说明缺口，不能编造诊断或引用。
- 不以“测试全通过”替代检索/回答质量；不以检索到候选替代有足够依据。所有完成声明给实际验证证据。

## 第一轮请做什么

先用简短文字确认你读到的当前状态和下一工作包，不重新长篇讲基础。检查计划与当前文件是否一致，然后开始D1：依据已有探针，准备完整开发集的单变量改进实验，冻结输入/计分并保留退步题；按计划选择最小实现，不同时更换模型、切块、数据库、检索和重排。

如果缺少模型运行信息，不影响的只读分析和本地实验继续进行；只在真正依赖时向我提问。不要一接手就重跑全部196项测试。

每次收尾告诉我：实际改了什么、验证结果和路径、仍存在的限制、开发工作包进度、学习单元进度、下一步。每日总结按学习计划保存Markdown，必要时配可离线打开的HTML。未观察到的用户回答不可编成真实学习记录。

现在请开始接手。
