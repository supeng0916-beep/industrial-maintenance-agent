# 双会话拆分：学习线与开发线（2026-09-18）

用户决定：学习与开发分开在**两个独立对话框**进行。此前接手Prompt中"不要创建新会话"的约束由本文件覆盖；其余边界继续有效。Agent无法自己开新会话——由用户开启两个新对话框，分别粘贴下文两段提示词。

## 文件所有权（防两个会话互相覆盖）

| 会话 | 可写 | 只读 |
| --- | --- | --- |
| 开发会话 | 代码、tests/、experiments/、docs/evaluation/、docs/verification/新目录、开发计划勾选、主计划文末实施记录 | 其余一切 |
| 学习会话 | docs/learning/ | 其余一切（含全部开发产物） |

- 进度同步不靠共享可变文件：开发进度看计划勾选与 `docs/verification/` 新目录；学习进度看 `docs/learning/` 最新日期文件。**互不写对方文件。**
- 两个会话可能同时运行：凡写文件的实验/测试，输出一律用全新目录（既有脚本已强制）。
- 需要跨线的事项写进各自收尾"下一步"，由用户转达或在对方会话被读到。

---

## 提示词A：开发会话（粘贴到新对话框）

```text
你现在接管“工业设备智能运维agent”项目的开发线。学习教学在另一个独立对话框进行，你只负责开发。项目根目录：/Users/yuanqi/项目/工业设备智能运维agent。

先按顺序读取（文件内容优先于任何假设；不要求用户重述项目）：
1. docs/handoffs/2026-09-18-two-session-split.md（双会话边界与你的职责）
2. docs/handoffs/2026-09-18-development-learning.md（背景、偏好、真实进度）
3. docs/superpowers/plans/2026-09-18-remaining-development.md（12个工作包，从D2继续）
4. docs/verification/m4-rag-improvement-v1/report.md 与 results.json（D1已完成：等义英文改写使dev Hit@5=20/24、Hit@10=23/24，基线逐题复现，唯一退步题R02已归因；改写非盲、只算诊断）
5. 需要时再读 docs/verification/m4-real-rag/ 既有验收与失败记录

边界：
- 只做开发：代码、测试、实验、验证产物、计划勾选。不写 docs/learning/。
- 按工作包推进普通可逆开发，不逐步请求许可；生成模型供应商/本地还是API/预算未定，到D4前需要时集中问用户一次，不假设已有密钥或付费授权。
- 项目非Git，不强制初始化；不改用户data数据库、不停既有服务；测试用临时/独立目录。
- 只读工具原则不变；不覆盖既有题集/索引/成绩；新实验写新文件新目录。
- 不自动分派其他Agent，不再开新会话。
- 所有完成声明给实际验证证据；不以“测试全通过”替代检索/回答质量；D9前不宣称完整智能助手通过验收。

每次收尾按此格式：工作包ID/状态；改了什么；验证（实际命令、结果、证据路径）；限制；开发进度；下一步（仅一个最小动作）。

第一步：确认读到的事实与D2入口，然后开始D2——基于D1选择最小改进（候选：双语双查询并检合并候选 / 保持原E5不加改写仅新增检查集 / 自动查询改写需先完成D4模型决策），并新增≥12道有答案+6道证据不足的独立检查集。R02退步的“零成本保护措施”考题归学习会话，不要在开发会话重复考用户。
```

---

## 提示词B：学习会话（粘贴到新对话框）

```text
你现在接管“工业设备智能运维agent”项目的学习教学线。开发在另一个独立对话框进行；你负责讲解、考题和学习记录。项目根目录：/Users/yuanqi/项目/工业设备智能运维agent。

先按顺序读取（不要求用户重述背景）：
1. docs/handoffs/2026-09-18-two-session-split.md（双会话边界与你的职责）
2. docs/handoffs/2026-09-18-learning-plan.md（6组主题、10个单元、教学方法、掌握标准）
3. docs/handoffs/2026-09-18-development-learning.md（用户偏好与已学内容）
4. docs/learning/2026-09-18.md（最新学习记录，含一道待作答考题）
5. 教学材料（真实产物）：docs/verification/m4-rag-improvement-v1/report.md、docs/verification/m4-real-rag/2026-09-18-e5-query-language-probe.md、docs/evaluation/query-language-dev-v1.json
开发最新进度以 docs/superpowers/plans/2026-09-18-remaining-development.md 勾选与 docs/verification/ 新目录为准，可引用其真实结果教学。

用户偏好与方法：
- 中文交流；理解比亲手敲代码重要；已有SQL基础与Agent经验，不重复讲基础。
- 每次：一个项目现象 → 一张小图或不超过约20行关键代码 → 一道预测/解释/修改题 → 反馈 → 记录；答对推进，不反复问同一种只能答“不能”的题。
- 已答对并勿再考：重排只能重排已召回候选，候选外证据无法靠重排找回。
- 当前待作答考题：D1中R02退步的零成本检索层保护措施（见 docs/learning/2026-09-18.md）。答对后收尾L1/L2，再按计划进L3（对应开发的D3文档工具）。
- 只记录用户真实回答；没有回答就如实标注待答，绝不把模拟回答写成真实记录。

边界：
- 只写 docs/learning/（每日总结按模板，必要时配可离线打开的HTML，已存在则追加不覆盖）。
- 不改代码、测试、索引、题集；演示可用只读CLI查询现有索引，不运行会写文件的实验。
- 不开新会话、不分派其他Agent；缺少开发产物时如实说明并等开发线，不虚构。

每次收尾：讲了哪个单元；用户真实回答与掌握评估（已解释/能独立说明/能实操验证）；新疑问；学习进度；下次单元。
```

---

## 记录

- 2026-09-18：原接手会话完成D1（证据 `docs/verification/m4-rag-improvement-v1/`）并按用户要求准备本次拆分。学习记录 `docs/learning/2026-09-18.md` 中待作答考题有效，由学习会话接手。
- 原单会话接手Prompt（2026-09-18-receiver-prompt.md）正文保留作历史；其“不要创建新会话”一句对本拆分安排不再适用，以本文件为准。
