# 开发与学习主任务交接

用户2026-09-18明确澄清：将使用自己的外部Agent接手，不要求Codex创建新任务。本文件是项目背景快照；交接总入口为 `docs/handoffs/README.md`，详细开发、学习安排及可复制Prompt由该入口链接。先核对实际文件，再小步推进，不从零讲起。

## 用户偏好与最新对话

- 中文交流，理解比亲手敲代码重要。已有SQL基础和Agent经验，不需反复讲基础SQL。
- 喜欢真实项目例子、关键代码/伪代码、解释后一道简短考题；答对后往前推进，避免长期停留在同一种“不能确定”判断题。
- 最新明确授权“按规划来，可以先开发再学习”。允许持续开发；不因普通可逆实现重复请求确认。涉及生成模型运行方式/凭据等未定信息时，提出必要的小问题。
- 希望真实机构/厂家资料，反对只用几份编造品牌/数据证明RAG效果。课程项目保持简单，不复用另一方向的旧Agent项目。
- 学习总结偏好独立HTML（图与伪代码）或Markdown，涵盖术语、疑问与回答；既有记录在docs/learning。2026-09-18的完整日总结尚未在本轮生成，勿声称已经生成。
- 最后一道题：正确表格排第8，只取前5再重排能否找回？用户已正确回答：重排只针对检索回来的内容，候选外的证据无法凭重排找回。无需重考。
- 用户同时询问还剩多少开发/学习；以真实阶段说明，不按章节数量冒充工作量百分比。

## 位置和协作

- 项目根目录 `/Users/yuanqi/项目/工业设备智能运维agent`，不是Git仓库；无需为了工作流初始化Git。
- 当前主计划 `docs/project-plan-v0.2.md`（文末日期记录为最新状态）。Documents/Codex下早期项目计划只是历史快照。
- 原主学习任务ID `01a0a450-d013-70e2-a0cd-2c7960892814`。
- 原开发任务ID `01a0a473-f212-76c2-9e38-ba5c552c3d61`，标题“首次开发：Modbus 温度模拟器与读取程序”，截至交接其本轮工作已完成。新任务接管主开发与教学，避免两个任务同时写相同文件。需要历史可读对应任务，不必重新创建一堆开发任务。
- 误创建的接手任务 `01a0b34a-0aa2-73b2-a02e-8348a2635e64` 已停止并归档。它仅新增只读查询探针报告 `docs/verification/m4-real-rag/2026-09-18-e5-query-language-probe.md`，未改业务代码、数据或默认配置；无遗留运行操作。该报告只是3道开发失败题的诊断，不能当全面提升或盲测成绩。
- 不随意终止本地既有服务或改写data中的学习数据库。历史服务曾用页面5192/API8022/Modbus15042/OPC UA4842，但这些不是当前已核实运行状态；需要时查实际进程/配置。

## 已完成

1. M0～M3：Modbus采集、SQLite历史/失败事件、API和看板；四点位；持续超温/回差/持久化去重；OPC UA读取/订阅、双设备隔离、质量和源时间校验、重连/防重复/积压时效。
2. M4.1：只读工具get_device_status、query_metric_history、list_alarms；业务历史数值从SQLite查询，不靠向量检索猜。
3. M4.2实验基线：真实PDF语料→提取/切块→真实嵌入→本地Chroma→CLI查询/44题评测。完成了实现与测量，**检索质量尚不够可靠**，没有LLM回答、自动拒答或助手UI。
4. 2026-09-18完整Python回归196项通过（84.103秒，真实嵌入测试启用）。原有前端本轮未改/未重测；不把历史33项前端通过说成此次重跑。

## 真实语料和结果（先读这些）

- `docs/verification/m4-real-rag/验收说明.md`：运行命令、签名、依赖、指标、限制。
- `docs/verification/m4-real-rag/检索结果与失败记录.md`、`集成复核.md`：失败/抽查。
- `docs/knowledge-real/manifest.json`：15份DOE Motor Systems Tip Sheets，30物理页，1机构1系列，2012年英文原文。不是多品牌手册或最新法规；未映射到motor-a/b，空device_ids不代表通用适用。
- 原PDF、提取和可视抽检 `docs/verification/m4-real-corpus/`；来源筛选（含HF为何未导入）`docs/research/2026-09-18-real-world-rag-sources.md`。
- 题集 `docs/evaluation/real-rag-v0.1.json`：44题，34有答案（24dev/10holdout），10证据不足。题目/答案不入索引。
- 最终索引 `docs/verification/m4-real-rag/doe-e5-final/`（113块）、`doe-bge-final/`（131块）。smoke/v1/v3为开发历史。
- E5 dev Hit@5=15/24、@10=19/24；holdout 3/10、5/10。BGE dev 4/24、7/24；holdout 2/10、3/10。
- 命中要求document/page/原文anchor；不是回答准确率。anchor并非穷尽所有等价证据。两个tokenizer切块不同，差异不能全归权重。保留集已被观察，后续优化应新增保留题，勿伪称原集合仍独立未见。
- 10道证据不足题保留候选、命中为null；N03/N06仅在集成复核中人工抽查，无自动拒答测试。
- E5 `intfloat/multilingual-e5-small`384维，BGE `BAAI/bge-small-zh-v1.5`512维，均完整输入512token上限；本地权重已缓存，无需重复下载。模型签名/版本严格匹配。
- 切块策略v4，300token只是软标记，实际512上限贪心装块；原文逐字覆盖不等于语义条件完整。TS11误继承局部条件已修复，TS07/08表前明确条件已关联。复杂跨页前提仍需原文核对。

可直接只读尝试：

```sh
.venv/bin/python -m assistant.rag query --index docs/verification/m4-real-rag/doe-e5-final --query '按典型PWM变频器效率表，20hp驱动器在12.5%负载时效率是多少？' --top-k 5
```

主任务已验证该题第一条TS11第2页；示例答案86%是典型表值，不是当前设备实测/指定品牌保证。

## 剩余开发：4块

1. M4.2质量改进：分析失败，区分语料缺失、跨语言、切块与排序；选一项小改动做可复现对照。E5是实验起点。BM25不能自动翻译中英文，不因“技术流行”同时加全部组件。新增中文厂家资料须保留型号/版本/许可/来源。
2. M4.3：选择生成模型/本地或API运行方式，按既定LangChain单Agent路线包装只读工具与检索工具；实现调用预算、超时、错误与证据不足语义。尚未选择供应商，也没有已授权的付费凭据配置，勿假定存在。
3. M4.4：现有前端添加助手，答案显示事实/推测/建议、引用、查询时间范围和证据时刻；完成工具/检索/回答分层验收与失败场景。
4. M5：统一启动复现、端到端演示、课程报告、最终学习者独立修改/排错/讲解。

## 剩余学习：6组核心主题

1. 实际检索失败定位和配置对照（召回/重排概念已懂，用实验深化）。
2. 单Agent工具调用流程、工具参数与职责。
3. 基于证据生成回答、引用和不确定性。
4. 超时、工具失败、无答案、提示注入等边界。
5. 助手前后端联调、端到端评测。
6. 独立复现/修改/排错与答辩讲解。

不是6道题即可完成；按实际掌握程度安排，开发可先行。不要虚构精确剩余天数。

## 首次接手建议

读交接总入口、本文件和上述验收/计划，简短说明接手位置。R17/R29/R34已有查询语言探针，应先审阅而不是重复相同实验。R03属于已观察holdout，可解释历史结果，但不要当开发题重新调参。优先按详细开发计划D1完成开发集改进实验；遵循当前环境可用的项目规则，必要技能不存在时按相同工程原则执行，不因没有Codex专用工具而阻塞。用户要外部Agent接手，勿创建Codex任务或要求重述整个项目。
