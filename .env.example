# 助手生成模型配置样例。复制为 .env 时只放真实值；本文件永远只放空值。
# 只由后端进程读取；浏览器/chat请求不能覆盖这些值。
# 路由规则：填了 ASSISTANT_API_KEY 走云API（OpenAI兼容端点）；否则走本地Ollama。
#
# —— 云API路线（免费档示例，以控制台/官网现价为准）——
# 智谱：open.bigmodel.cn 控制台获取key；免费档模型名以控制台为准（如glm-4-flash）
#   ASSISTANT_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
# DeepSeek等其它OpenAI兼容供应商：改BASE_URL与模型名即可，适配器通用。
#
# —— 本地Ollama路线（零云成本）——
#   仅填 ASSISTANT_MODEL=qwen2.5:7b 并保持Ollama服务运行即可，无需key。
ASSISTANT_MODEL=
# 云API路线必填（不填key时忽略）；本地路线可留空（默认127.0.0.1:11434）
ASSISTANT_BASE_URL=
# 仅云API需要；本地Ollama留空
ASSISTANT_API_KEY=
# 可选覆盖：助手用的文档索引与业务库路径（默认用仓库内最终索引/默认业务库）
# ASSISTANT_INDEX=
# ASSISTANT_DB=
